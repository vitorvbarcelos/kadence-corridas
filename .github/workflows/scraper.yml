- name: Install required system dependencies
  run: |
    sudo apt-get update
    sudo apt-get install -y \
      libasound2t64 \
      libatk-bridge2.0-0 \
      libatk1.0-0 \
      libdbus-1-3 \
      libgdk-pixbuf2.0-0 \
      libnspr4 \
      libnss3 \
      libx11-xcb1 \
      libx264-dev \
      libffi-dev
